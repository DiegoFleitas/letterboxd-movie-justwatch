# dependencies
/node_modules
.pnpm-store

# production build files
src/client/dist

# local env files
.env
.env.*

# editor and IDE files
.vscode/

# AI agent tooling — local-only (skills, agents, opencode config).
# Keep the shared Claude settings; everything else under these dirs is local.
.agents/
.claude/*
!.claude/settings.json
.opencode/

# misc
*.log
*.har
# Large optional Redis dump (canonical JSON stays tracked)
/redis/data/redis-snapshot.json


.npmrc
.corepack

# Playwright (e2e) output
/tests/playwright-report/
/tests/test-results/

# Legacy Playwright default paths (before reports lived at repo root)
/playwright-report/
/test-results/
coverage/
