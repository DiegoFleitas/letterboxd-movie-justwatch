# Fixtures

`api/letterboxd-watchlist.json` and `api/search-movie.json` — request/response mocks for local use.

`letterboxd-watchlist-page.html` and `letterboxd-list-fragment.html` — HTML snippets for list scraper unit tests. To refresh from live Letterboxd pages, run: `pnpm run update:letterboxd-fixtures`.
