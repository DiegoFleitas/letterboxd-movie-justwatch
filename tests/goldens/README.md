This directory contains golden response fixtures for backend integration tests.

At this stage, the integration tests focus primarily on schema-level assertions.
You can add stable, deterministic JSON snapshots here for endpoints where
exact response parity for regressions is important.
