# Configuration

The authoritative list of variables is **`.env.example`** in the repository root.

| Variable                                              | Notes                                                                                                                                                                                                                                                                                            |
| ----------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| `APP_SECRET_KEY`                                      | **Required in production**; ≥ 32 characters (Fastify session). Local `bun run dev` may use a dev default if unset.                                                                                                                                                                               |
| `FLYIO_REDIS_URL`                                     | e.g. `redis://localhost:6379`. Avoid pointing production deploys at local Redis—see [Redis and local dev](Redis-and-local-dev) (in-repo [`redis/README.md`](https://github.com/DiegoFleitas/letterboxd-movie-justwatch/blob/master/redis/README.md)).                                            |
| `DISABLE_REDIS`                                       | `1` / `true` skips Redis (typical in CI).                                                                                                                                                                                                                                                        |
| `OMDB_API_KEY`                                        | Poster lookups.                                                                                                                                                                                                                                                                                  |
| `MOVIE_DB_API_KEY`                                    | TMDb / search; optional locally; enables extra integration coverage in CI when set.                                                                                                                                                                                                              |
| `POSTHOG_KEY` / `POSTHOG_HOST`                        | Optional analytics. `POSTHOG_HOST` defaults to the app's first-party reverse-proxy path. Use `POSTHOG_HOST=/phc4v7x` for US Cloud (recommended), switch upstream mapping for EU if needed, or set direct ingest host (`https://us.i.posthog.com` / `https://eu.i.posthog.com`) only as fallback. |
| `SENTRY_DSN` / `SENTRY_*`                             | Optional frontend and backend Sentry ([Sentry and logger](Sentry-and-logger)).                                                                                                                                                                                                                   |
| `SENTRY_AUTH_TOKEN` / `SENTRY_ORG` / `SENTRY_PROJECT` | Required in CI for `sentry-cli` source map upload.                                                                                                                                                                                                                                               |
| `JACKETT_API_KEY` / `JACKETT_API_ENDPOINT`            | Optional alternative search.                                                                                                                                                                                                                                                                     |
| `CACHE_TTL`                                           | Optional cache TTL override (seconds).                                                                                                                                                                                                                                                           |
| `PORT`                                                | Backend port (default **3000**).                                                                                                                                                                                                                                                                 |
| `E2E_API_BASE_URL`                                    | Playwright backend smoke base URL (default `http://127.0.0.1:3000`).                                                                                                                                                                                                                             |
